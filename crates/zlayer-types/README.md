# zlayer-types

Shared wire types for the ZLayer platform — API DTOs, OCI image
references, and related serde types. Designed to be the lightweight
crate that SDKs and external integrations depend on.
