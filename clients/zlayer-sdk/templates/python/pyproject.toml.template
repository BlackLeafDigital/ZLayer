[project]
name = "{{name}}"
version = "{{version}}"
description = "{{description}}"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["componentize-py>=0.13"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
