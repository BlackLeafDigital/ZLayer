{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "type": "module",
  "scripts": {
    "build": "jco componentize src/plugin.js --wit wit -o {{name}}.wasm",
    "clean": "rm -f *.wasm"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.4.0",
    "@zlayer/sdk": "^0.1.0",
    "typescript": "^5.3.0"
  }
}
