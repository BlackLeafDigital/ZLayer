/* tslint:disable */
/* eslint-disable */
/**
 * ZLayer API
 * Container orchestration API for ZLayer
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkImportResponse,
  CreateSecretRequest,
  SecretMetadataResponse,
} from '../models/index';
import {
    BulkImportResponseFromJSON,
    BulkImportResponseToJSON,
    CreateSecretRequestFromJSON,
    CreateSecretRequestToJSON,
    SecretMetadataResponseFromJSON,
    SecretMetadataResponseToJSON,
} from '../models/index';

export interface BulkImportSecretsRequest {
    environment: string;
    body: string;
}

export interface CreateSecretOperationRequest {
    createSecretRequest: CreateSecretRequest;
    environment?: string;
    scope?: string;
}

export interface DeleteSecretRequest {
    name: string;
    environment?: string;
    scope?: string;
}

export interface GetSecretMetadataRequest {
    name: string;
    environment?: string;
    scope?: string;
    reveal?: boolean;
}

export interface ListSecretsRequest {
    environment?: string;
    scope?: string;
}

/**
 * 
 */
export class SecretsApi extends runtime.BaseAPI {

    /**
     * Creates request options for bulkImportSecrets without sending the request
     */
    async bulkImportSecretsRequestOpts(requestParameters: BulkImportSecretsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['environment'] == null) {
            throw new runtime.RequiredError(
                'environment',
                'Required parameter "environment" was null or undefined when calling bulkImportSecrets().'
            );
        }

        if (requestParameters['body'] == null) {
            throw new runtime.RequiredError(
                'body',
                'Required parameter "body" was null or undefined when calling bulkImportSecrets().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['environment'] != null) {
            queryParameters['environment'] = requestParameters['environment'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'text/plain';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/secrets/bulk-import`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        };
    }

    /**
     * Each non-empty, non-comment line is parsed into a (name, value) pair and written to the env\'s scope. Lines that fail to parse are returned in `errors` and do not abort the import. Each successful write is counted as either `created` or `updated`.  # Errors  Returns [`ApiError::Forbidden`] for non-admins, [`ApiError::NotFound`] when the environment id is unknown, or [`ApiError::Internal`] when the secrets store fails.
     * Bulk-import secrets from a dotenv-style payload (`KEY=value\\n…`).
     */
    async bulkImportSecretsRaw(requestParameters: BulkImportSecretsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkImportResponse>> {
        const requestOptions = await this.bulkImportSecretsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkImportResponseFromJSON(jsonValue));
    }

    /**
     * Each non-empty, non-comment line is parsed into a (name, value) pair and written to the env\'s scope. Lines that fail to parse are returned in `errors` and do not abort the import. Each successful write is counted as either `created` or `updated`.  # Errors  Returns [`ApiError::Forbidden`] for non-admins, [`ApiError::NotFound`] when the environment id is unknown, or [`ApiError::Internal`] when the secrets store fails.
     * Bulk-import secrets from a dotenv-style payload (`KEY=value\\n…`).
     */
    async bulkImportSecrets(requestParameters: BulkImportSecretsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkImportResponse> {
        const response = await this.bulkImportSecretsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createSecret without sending the request
     */
    async createSecretRequestOpts(requestParameters: CreateSecretOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createSecretRequest'] == null) {
            throw new runtime.RequiredError(
                'createSecretRequest',
                'Required parameter "createSecretRequest" was null or undefined when calling createSecret().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['environment'] != null) {
            queryParameters['environment'] = requestParameters['environment'];
        }

        if (requestParameters['scope'] != null) {
            queryParameters['scope'] = requestParameters['scope'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/secrets`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateSecretRequestToJSON(requestParameters['createSecretRequest']),
        };
    }

    /**
     * Stores a new secret or updates an existing one. The secret value is encrypted at rest and the version number is incremented on updates.  Scope resolution: see [`resolve_scope`].  # Errors  Returns an error if validation fails, storage operations fail, or the caller lacks permission.
     * Create or update a secret.
     */
    async createSecretRaw(requestParameters: CreateSecretOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SecretMetadataResponse>> {
        const requestOptions = await this.createSecretRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SecretMetadataResponseFromJSON(jsonValue));
    }

    /**
     * Stores a new secret or updates an existing one. The secret value is encrypted at rest and the version number is incremented on updates.  Scope resolution: see [`resolve_scope`].  # Errors  Returns an error if validation fails, storage operations fail, or the caller lacks permission.
     * Create or update a secret.
     */
    async createSecret(requestParameters: CreateSecretOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SecretMetadataResponse> {
        const response = await this.createSecretRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteSecret without sending the request
     */
    async deleteSecretRequestOpts(requestParameters: DeleteSecretRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteSecret().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['environment'] != null) {
            queryParameters['environment'] = requestParameters['environment'];
        }

        if (requestParameters['scope'] != null) {
            queryParameters['scope'] = requestParameters['scope'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/secrets/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Scope resolution: see [`resolve_scope_get`] (the same query type is reused minus `reveal`).  # Errors  Returns an error if the secret is not found, storage access fails, or the caller lacks permission.
     * Delete a secret.
     */
    async deleteSecretRaw(requestParameters: DeleteSecretRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteSecretRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Scope resolution: see [`resolve_scope_get`] (the same query type is reused minus `reveal`).  # Errors  Returns an error if the secret is not found, storage access fails, or the caller lacks permission.
     * Delete a secret.
     */
    async deleteSecret(requestParameters: DeleteSecretRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSecretRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getSecretMetadata without sending the request
     */
    async getSecretMetadataRequestOpts(requestParameters: GetSecretMetadataRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling getSecretMetadata().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['environment'] != null) {
            queryParameters['environment'] = requestParameters['environment'];
        }

        if (requestParameters['scope'] != null) {
            queryParameters['scope'] = requestParameters['scope'];
        }

        if (requestParameters['reveal'] != null) {
            queryParameters['reveal'] = requestParameters['reveal'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/secrets/{name}`;
        urlPath = urlPath.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Scope resolution: see [`resolve_scope_get`].  # Errors  Returns an error if the secret is not found, the caller is unauthorised to reveal, or storage access fails.
     * Get metadata for a specific secret. With `?reveal=true` (admin only), the response also includes the plaintext `value`.
     */
    async getSecretMetadataRaw(requestParameters: GetSecretMetadataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SecretMetadataResponse>> {
        const requestOptions = await this.getSecretMetadataRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SecretMetadataResponseFromJSON(jsonValue));
    }

    /**
     * Scope resolution: see [`resolve_scope_get`].  # Errors  Returns an error if the secret is not found, the caller is unauthorised to reveal, or storage access fails.
     * Get metadata for a specific secret. With `?reveal=true` (admin only), the response also includes the plaintext `value`.
     */
    async getSecretMetadata(requestParameters: GetSecretMetadataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SecretMetadataResponse> {
        const response = await this.getSecretMetadataRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listSecrets without sending the request
     */
    async listSecretsRequestOpts(requestParameters: ListSecretsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['environment'] != null) {
            queryParameters['environment'] = requestParameters['environment'];
        }

        if (requestParameters['scope'] != null) {
            queryParameters['scope'] = requestParameters['scope'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/secrets`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Scope resolution: see [`resolve_scope`].  # Errors  Returns an error if storage access fails.
     * List secrets in a scope.
     */
    async listSecretsRaw(requestParameters: ListSecretsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SecretMetadataResponse>>> {
        const requestOptions = await this.listSecretsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SecretMetadataResponseFromJSON));
    }

    /**
     * Scope resolution: see [`resolve_scope`].  # Errors  Returns an error if storage access fails.
     * List secrets in a scope.
     */
    async listSecrets(requestParameters: ListSecretsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SecretMetadataResponse>> {
        const response = await this.listSecretsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
