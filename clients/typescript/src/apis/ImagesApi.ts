/* tslint:disable */
/* eslint-disable */
/**
 * ZLayer API
 * Container orchestration API for ZLayer
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ImageInfoDto,
  PruneResultDto,
  PullImageRequest,
  PullImageResponse,
  TagImageRequest,
} from '../models/index';
import {
    ImageInfoDtoFromJSON,
    ImageInfoDtoToJSON,
    PruneResultDtoFromJSON,
    PruneResultDtoToJSON,
    PullImageRequestFromJSON,
    PullImageRequestToJSON,
    PullImageResponseFromJSON,
    PullImageResponseToJSON,
    TagImageRequestFromJSON,
    TagImageRequestToJSON,
} from '../models/index';

export interface PullImageHandlerRequest {
    pullImageRequest: PullImageRequest;
}

export interface RemoveImageHandlerRequest {
    image: string;
    force?: boolean;
}

export interface TagImageHandlerRequest {
    tagImageRequest: TagImageRequest;
}

/**
 * 
 */
export class ImagesApi extends runtime.BaseAPI {

    /**
     * Creates request options for listImagesHandler without sending the request
     */
    async listImagesHandlerRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/images`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * # Errors  Returns an error if authentication fails or the runtime cannot enumerate its image cache (for example, when the backend does not implement `list_images`).
     * List all cached images known to the runtime.
     */
    async listImagesHandlerRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ImageInfoDto>>> {
        const requestOptions = await this.listImagesHandlerRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ImageInfoDtoFromJSON));
    }

    /**
     * # Errors  Returns an error if authentication fails or the runtime cannot enumerate its image cache (for example, when the backend does not implement `list_images`).
     * List all cached images known to the runtime.
     */
    async listImagesHandler(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ImageInfoDto>> {
        const response = await this.listImagesHandlerRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for pruneImagesHandler without sending the request
     */
    async pruneImagesHandlerRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/system/prune`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * # Errors  Returns an error if authentication fails or the runtime backend does not support pruning.
     * Prune dangling / unused images from the runtime\'s cache.
     */
    async pruneImagesHandlerRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PruneResultDto>> {
        const requestOptions = await this.pruneImagesHandlerRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PruneResultDtoFromJSON(jsonValue));
    }

    /**
     * # Errors  Returns an error if authentication fails or the runtime backend does not support pruning.
     * Prune dangling / unused images from the runtime\'s cache.
     */
    async pruneImagesHandler(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PruneResultDto> {
        const response = await this.pruneImagesHandlerRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for pullImageHandler without sending the request
     */
    async pullImageHandlerRequestOpts(requestParameters: PullImageHandlerRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['pullImageRequest'] == null) {
            throw new runtime.RequiredError(
                'pullImageRequest',
                'Required parameter "pullImageRequest" was null or undefined when calling pullImageHandler().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/images/pull`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PullImageRequestToJSON(requestParameters['pullImageRequest']),
        };
    }

    /**
     * This is a blocking pull: the handler returns only after the image is resolved and stored locally (or the pull fails). When `pull_policy` is omitted the default is `\"always\"`, matching Docker-compat semantics for `POST /images/create`. On success, the response echoes the reference and best-effort `digest`/`size_bytes` resolved via `list_images()`.  # Errors  Returns an error if authentication fails, the reference is empty, the image cannot be pulled, or the user lacks the operator role.
     * Pull an OCI image into the runtime\'s local cache.
     */
    async pullImageHandlerRaw(requestParameters: PullImageHandlerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PullImageResponse>> {
        const requestOptions = await this.pullImageHandlerRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PullImageResponseFromJSON(jsonValue));
    }

    /**
     * This is a blocking pull: the handler returns only after the image is resolved and stored locally (or the pull fails). When `pull_policy` is omitted the default is `\"always\"`, matching Docker-compat semantics for `POST /images/create`. On success, the response echoes the reference and best-effort `digest`/`size_bytes` resolved via `list_images()`.  # Errors  Returns an error if authentication fails, the reference is empty, the image cannot be pulled, or the user lacks the operator role.
     * Pull an OCI image into the runtime\'s local cache.
     */
    async pullImageHandler(requestParameters: PullImageHandlerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PullImageResponse> {
        const response = await this.pullImageHandlerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for removeImageHandler without sending the request
     */
    async removeImageHandlerRequestOpts(requestParameters: RemoveImageHandlerRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['image'] == null) {
            throw new runtime.RequiredError(
                'image',
                'Required parameter "image" was null or undefined when calling removeImageHandler().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['force'] != null) {
            queryParameters['force'] = requestParameters['force'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/images/{image}`;
        urlPath = urlPath.replace(`{${"image"}}`, encodeURIComponent(String(requestParameters['image'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * # Errors  Returns an error if authentication fails, the image cannot be found, or the runtime backend does not support image removal.
     * Remove an image from the runtime\'s cache.
     */
    async removeImageHandlerRaw(requestParameters: RemoveImageHandlerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.removeImageHandlerRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * # Errors  Returns an error if authentication fails, the image cannot be found, or the runtime backend does not support image removal.
     * Remove an image from the runtime\'s cache.
     */
    async removeImageHandler(requestParameters: RemoveImageHandlerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.removeImageHandlerRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for tagImageHandler without sending the request
     */
    async tagImageHandlerRequestOpts(requestParameters: TagImageHandlerRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['tagImageRequest'] == null) {
            throw new runtime.RequiredError(
                'tagImageRequest',
                'Required parameter "tagImageRequest" was null or undefined when calling tagImageHandler().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/images/tag`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TagImageRequestToJSON(requestParameters['tagImageRequest']),
        };
    }

    /**
     * Docker-compat `POST /api/v1/images/tag`: takes `{ source, target }` and asks the runtime to make `target` resolve to the same content as `source`. Both references must be non-empty; `target` is split on the last `:` into repository + tag (defaulting tag to `latest`).  # Errors  Returns `400` if the request body is malformed, `404` if the source image is not in the cache, `403` if the caller lacks the `operator` role, `501` if the runtime does not support tagging, and `500` for other runtime errors.
     * Create a new tag pointing at an existing image.
     */
    async tagImageHandlerRaw(requestParameters: TagImageHandlerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.tagImageHandlerRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Docker-compat `POST /api/v1/images/tag`: takes `{ source, target }` and asks the runtime to make `target` resolve to the same content as `source`. Both references must be non-empty; `target` is split on the last `:` into repository + tag (defaulting tag to `latest`).  # Errors  Returns `400` if the request body is malformed, `404` if the source image is not in the cache, `403` if the caller lacks the `operator` role, `501` if the runtime does not support tagging, and `500` for other runtime errors.
     * Create a new tag pointing at an existing image.
     */
    async tagImageHandler(requestParameters: TagImageHandlerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.tagImageHandlerRaw(requestParameters, initOverrides);
    }

}
